#include <bits/stdc++.h>
using namespace std;
class Solution{
public:
	int arrayPairSum(vector<int>& nums)
	{
		sort(nums.begin(),nums.end());
		int maxsum =0 ;
		for(int i=0;i<num.size()-1;iter+=2)
		{
			maxsum += nums[i];
		}
		return maxsum;
	}
	
}

int main()
{
	
	return 0;
}
